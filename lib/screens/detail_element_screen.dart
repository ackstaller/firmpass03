import "package:flutter/material.dart";

class DetailElementScreen extends StatelessWidget {
  const DetailElementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("DetailPetScreen")),
    );
  }
}
